#version 330 core

// Input vertex data, different for all executions of this shader.
in vec3 position;
in vec3 color;
out vec3 Color;
void main(){
    Color = color;
    gl_Position = vec4(1*position, 1.0);
}

